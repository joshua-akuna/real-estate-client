import RegisterForm from '@/components/forms/RegisterForm';

export const metadata = {
  title: 'Register | Real Estate App',
};

export default function RegisterPage() {
  return <RegisterForm />;
}
